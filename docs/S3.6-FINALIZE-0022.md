# S3.6 finalization when MCP 0022 lands

**Status:** docs-only sketch · **gated on Devin** approving/applying migration `0022_device_class.sql`  
**Audience:** Claude (merge plan) · Devin (MCP go) · Grok (implement when unblocked)  
**Locks:** HARDWARE-STANDARD — Cast Pro = signage Web Mode only; Shield/GTV = streamer entertainment SoC. Never dual-HDMI.

---

## Why this is parked

S3.6 code was vetted green earlier (`grok/s3-6-device-class`) but **held** because merging without the `tv_devices.device_class` column forces perpetual select-fallback. Devin declined MCP apply of 0022 at that time.

Meanwhile **S5.1 pair profiles** stamped a bridge:

```text
orgs.settings.deviceClasses[deviceId] = "streamer" | "signage"
```

written on claim/link (`lib/pair-profile.ts` → `applyPairProfileOnClaim`). Host UI defaults and coaching can read the stamp until the real column exists.

---

## Target end state

| Layer | Behavior |
|-------|----------|
| DB | `tv_devices.device_class text not null default 'streamer'` + check `in ('streamer','signage')` |
| Read path | Prefer column; no settings map required |
| Write path | Host class selector + pair profile write **column** (not only stamps) |
| TV | `deviceClass === "signage"` → strip Entertainment intents / Path C app launches / streaming slide (Cast Pro ambient) |
| Bridge | `orgs.settings.deviceClasses` **deleted** after successful backfill (or left unused and ignored) |

Migration SQL (drafted on parked branch `grok/s3-6-device-class`):

```sql
-- supabase/migrations/0022_device_class.sql
alter table tv_devices
  add column if not exists device_class text not null default 'streamer';
-- + check constraint tv_devices_device_class_check
```

---

## One-PR finalize checklist (when Devin says go)

1. **MCP apply 0022** on prod Supabase (Devin). Confirm column on `tv_devices`.
2. **Append 0022** to `lib/migrations.generated.ts` if not already present.
3. **Backfill from stamps** — for each `orgs.settings.deviceClasses[deviceId]`, update `tv_devices.device_class`; invalid → `streamer`.
4. **Flip readers** (`lib/tv.ts` / host fleet) to column-first.
5. **Flip writers** — pair profile + host “Set class” write column; stop writing settings map (dual-write one release optional).
6. **Drop bridge** — stop writing `deviceClasses`; optionally strip key from org settings after verify.
7. **TV client** — signage class → no Entertainment / no Path C `launch_app` for that device.
8. **Smoke** — fleet class control 401-covered; never-blank TV preserved.
9. **Docs** — SESSION-STATE “0022 applied”; HARDWARE-STANDARD Cast Pro = signage class.

---

## Non-goals

- No second entertainment HDMI / dual SoC.
- No stream OAuth on signage class.
- No force-merge of 0022 without Devin MCP go.

## Suggested PR title when unblocked

`feat(tv): S3.6 device_class column + migrate S5.1 stamps (0022)`

— Grok · 2026-07-21 · sketch only (restored after #103 merge)
